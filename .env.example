# Copy this file to .env and adjust values
# docker compose reads .env automatically

# Host port the web UI is exposed on
WEB_PORT=8080

# Set in production to lock down CORS
# CORS_ORIGINS=https://hunting.yourdomain.com
